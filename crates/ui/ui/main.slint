// FluxDM - Main Window
import { VerticalBox, HorizontalBox, Button, LineEdit, ProgressIndicator } from "std-widgets.slint";

export component MainWindow inherits Window {
    title: "FluxDM - Download Manager";
    preferred-width: 600px;
    preferred-height: 200px;
    
    background: #1E1E1E;

    VerticalBox {
        padding: 16px;
        spacing: 12px;
        
        // Header section
        Text {
            text: "FluxDM Download Manager";
            font-size: 18px;
            font-weight: 700;
            color: #CCCCCC;
        }
        
        // URL input section
        HorizontalBox {
            spacing: 8px;
            
            LineEdit {
                placeholder-text: "Enter URL...";
                font-size: 14px;
            }
            
            Button {
                text: "Start Download";
                primary: true;
            }
        }
        
        // Download info section (placeholder)
        Rectangle {
            background: #252526;
            border-radius: 4px;
            height: 80px;
            
            VerticalBox {
                padding: 12px;
                spacing: 8px;
                
                Text {
                    text: "📁 No active download";
                    font-size: 14px;
                    color: #CCCCCC;
                }
                
                HorizontalBox {
                    spacing: 4px;
                    
                    Rectangle {
                        background: #007ACC;
                        height: 8px;
                        width: 50%;
                        border-radius: 4px;
                    }
                }
                
                Text {
                    text: "Ready to download";
                    font-size: 12px;
                    color: #858585;
                }
            }
        }
    }
}
